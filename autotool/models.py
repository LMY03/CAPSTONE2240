from django.db import models

# Create your models here.
# class Host(models.Model):
#     ip_address = models.CharField(max_length=15, unique=True, help_text="IP address of the host")
#     vm_user = models.CharField(max_length=50, help_text="Ansible user for the host")
#     vm_name = models.CharField(max_length=100, help_text="Hostname for identification")
#     type = models.CharField(max_length=100, help_text="Label for the host")